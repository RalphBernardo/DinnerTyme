<%- model_class = Ingredient -%>
<div class="page-header">
  <h2><%=t '.title', :default => model_class.model_name.human.pluralize %>&nbsp; &nbsp;

  <%= link_to 'Create New', new_ingredient_path, :class => 'btn btn-primary' %></h2>
</div>

<%= render :partial => 'shared/ingredient', :collection => @ingredients, :as => :ingredient %>

<div class="span12 form-actions">
  <%= link_to 'Create New Ingredient', new_ingredient_path, :class => 'btn btn-primary' %>
  &nbsp;&nbsp;
  <%= link_to 'See All Recipes', recipes_path, :class => 'btn btn' %>
  &nbsp;&nbsp;
  <%= link_to 'See All Kitchens', kitchens_path, :class => 'btn btn' %>
</div>
