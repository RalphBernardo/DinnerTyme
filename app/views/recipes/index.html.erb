<%- model_class = Recipe -%>
<div class="page-header">
  <h2><%=t '.title', :default => model_class.model_name.human.pluralize %>&nbsp; &nbsp;

  <%= link_to 'Create New', new_recipe_path, :class => 'btn btn-primary' %></h2>
</div>

<%= render :partial => 'shared/recipe', :collection => @recipes, :as => :recipe %>

<div class="span12 form-actions">
  <%= link_to 'Create New Recipe', new_recipe_path, :class => 'btn btn-primary' %>
  &nbsp;&nbsp;
  <div class="btn-group">
    <button class="btn">See All</button>
    <button class="btn dropdown-toggle" data-toggle="dropdown">
      <span class="caret"></span>
    </button>
    <ul class="dropdown-menu">
      <li><%= link_to 'Ingredients', ingredients_path %></li>
      <li class="divider"></li>
      <li><%= link_to 'Kitchens', kitchens_path %></li>
    </ul>
  </div>
</div>
